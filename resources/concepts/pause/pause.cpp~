//A pause test to develop pause lengths and implementations within the program.

#include <iostream>
#include <unistd.h>
using namespace std;

int main()
{
  for(int i=0; i<3; i++)
    {
      cout << ". "; 
      sleep(100);
    }
  cout << "\nDONE" << endl;

  return 0;
}
